apiVersion: v1
kind: ResourceQuota
metadata:
  name: petclinic-resource-quota
  namespace: petclinic-prod
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 3Gi
    limits.cpu: "4"
    limits.memory: 6Gi
    pods: "10"
    services: "10"
    persistentvolumeclaims: "5"